# Skip the pre-script dep-freshness check. Our deps are workspace-linked
# to a sibling repo and pnpm's install-check tries to re-run install
# every time, tripping on unapproved native build scripts we don't need.
verify-deps-before-run=false

# Disable the dependency status check that runs before each script.
run-script-shell=powershell
